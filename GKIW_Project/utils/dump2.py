import os
import io
from moderngl_window.loaders.scene.gltf2 import glTF2

# We need a context to load? glTF2 scene loading might require gl context.
# Actually, wait. We can just run main.py but dumping debug to a file.
